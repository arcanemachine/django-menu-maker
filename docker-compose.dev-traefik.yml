---
version: "3.8"

services:
  django:
    networks:
      - traefik-global-proxy
    expose:
      - "${PROJECT_PORT_INTERNAL}"
    command: >
      bash -c "./1--secret-key-create-if-not-exists &&
               ./2--server-config-choose dev &&
               python3 manage.py migrate &&
               python3 manage.py collectstatic --no-input
               gunicorn -b 0.0.0.0:${PROJECT_PORT_INTERNAL} $PROJECT_NAME_PYTHON.wsgi"

  nginx:
    image: nginx
    depends_on:
      - django
    networks:
      - traefik-global-proxy
    expose:
      - "${PROJECT_PORT_EXTERNAL}"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./app/staticfiles:/staticfiles:ro
      - ./app/media:/media:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${PROJECT_NAME}.rule=Host(`${DEV_DOMAIN_NAME}`)"
      - "traefik.http.routers.${PROJECT_NAME}.entrypoints=web"

networks:
  traefik-global-proxy:
    external: true
